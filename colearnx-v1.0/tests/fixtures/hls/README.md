Synthetic 14-second, silent H.264/fMP4 recording of a canvas created locally for the HLS browser regression test. Contains no third-party media or user data. The init segment and media fragments were split from Chromium MediaRecorder output; the playlist is a local VOD fixture. This exercises real hls.js decoding with mocked API and media delivery, not a production Gateway or FFmpeg pipeline.
